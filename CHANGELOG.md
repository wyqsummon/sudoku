# 开发记录

按开发阶段倒序排列。每个阶段列出「做了什么」「实测数据」「修掉的坑」。

## 阶段九 · 十七数题型 + 高阶技巧 + 教程与专项练习

**新增**

- **「十七数」难度**：题面恰好 17 个提示数（唯一解的理论下限）。随机挖洞挖不出这种题
  （单格变异约 2500 次 0 命中、退火 10.7 万次迭代 54 个唯一解但无一高于 2.3 分），
  改为**内置母题 + 等价变换**（换数字、换行带/列带、带内换行换列、可选转置）。
- **四种高阶技巧**：ALS-XZ（级 11）、Sue de Coq（12）、ALS 链（13）、连续环（13），接入求解器与提示管线。
- **「技巧教程与专项练习」页**：按 基础（≤2 级）/ 进阶（3~6 级）/ 高阶（≥7 级）分组，每种技巧一页
  （一句话说明 + 原理 + 怎么找 + 怎么做 + 一道真实例题），一键把盘面停在「只剩这一招」的卡点上练手。
- 主菜单档位改成两行三列：简单 / 中等 / 困难 ｜ 专家 / 大师 / 十七数。

**十七数题库改为只收「必须用到高阶技巧」的题目**

公开的 17 提示数全目录里绝大多数其实是简单题（49157 道里 21932 道只需唯一候选数），
于是逐道评测全目录（16 线程 7.5 分钟）后按技巧分组内置 24 道母题：

| 分类 | 道数 |
| --- | --- |
| 全目录 | 49157 |
| 用到高阶技巧 | 2932 |
| 纯逻辑可解且用到高阶技巧（＝候选） | 1915 |
| 纯逻辑解不完 | 1107 |

母题按最难技巧各取 4 道：ALS 链（13 级，9.2 分）、ALS-XZ（11，8.8~9.0）、BUG+1（10，8.6）、
唯一矩形（8，6.7）、带鳍 X 翼（8，6.6）、远程数对（7，6.4）。
档位判定要求「17 个提示数 **且** 已到大师档」两个条件同时成立，因此难度系数稳定落在 6.4 ~ 9.2。
详见 [`docs/TECHNIQUES.md`](docs/TECHNIQUES.md#四十七数题型)。

**修掉的坑**

- **Sue de Coq 的锁定集判定**：网上流行的「宫侧格数 + 线侧格数 − 交叉格数 = 合计候选数个数」写法是错的，
  会删掉正确答案（复现 5 例）。改成要求**每一侧各自**构成锁定集后普查 985 次命中、**失真 0 次**。
- **「两侧共有候选数 ⇒ 只能落在交叉格」也是错的**（会删掉正解 R2C3(7)、R1C5(7)）。
- 十七数题目会逼出 13 级技巧，提示的「技法上限」若仍停在 10 就会永远显示「没有找到可用技巧」，
  因此十七数对局会自动放宽到 13 级。

**实测**（Windows 绿色版，UIA 元素几何 + 存档核对）

| 检查项 | 结果 |
| --- | --- |
| 主菜单档位 | 六个按钮两行三列：简单/中等/困难 @ y=410，专家/大师/十七数 @ y=466，各 267×50 |
| 教程入口 | 三个分组齐全：基础技巧（2 种）/ 进阶技巧（9 种）/ 高阶技巧（12 种） |
| 教程详情 | 原理 / 怎么找 / 怎么做 / 例题四段齐全；ALS-XZ 页出现「专项练习」按钮 |
| 专项练习开局 | 顶部等级标签变成「练习 · ALS-XZ（准锁定集）」，状态栏给出卡点说明 |
| 练习里点提示 | 提示面板标题**直接就是**「ALS-XZ（准锁定集）」（练习模式自动放宽技法上限） |
| 十七数出题 | 连开三局「十七数 · 6.6」「十七数 · 6.6」「十七数 · 8.6」；存档 17 个提示数、`LevelValue=5`、`TechniqueLevel` 8/8/10 |
| 十七数提示 | 提示面板列出「X 链（3 条）/ ALS-XZ（4 条）/ Sue de Coq（2 条）」 |
| 练习不污染存档 | 练习局 `SaveGame()` 直接返回，「继续上一局」不被覆盖 |

## 阶段八 · 手机竖屏纵向空间

**问题**：棋盘偏小、上下两排按钮之间空一大截——因为「绘制面板在窄屏会折成两行」，
底部整块的高度取的是两者较大者，于是普通模式也白留两行高度（400×800 窗口下实测空出 49 px）。

**改动**：绘制模式收起数字键盘；对局页去掉导航栏；窄屏压矮按钮与字号、棋盘内边距 10 → 2；
状态栏仍按最长状态文字预留高度。

| 窗口 | 棋盘尺寸 | 说明 |
| --- | --- | --- |
| 400×800（内容 404×792） | **400×400** | 几乎满宽，只剩 2 px 边距（改之前约 340） |
| 460×900（内容 444×892） | 440×440 | 同上 |
| 700×1000 | 636×632 | 高度受限 |
| 1000×1040 | 666×662 | 高度受限 |

400×800 下依次点格子、笔记开/关、XY 开/关、自动标记开/关、进/出绘制模式共 11 个状态，
棋盘包围盒**始终是 `400x400 @ (2,129)`**。

## 阶段七 · 棋盘尺寸稳定 + XY 双值按钮 + 绘图不被高亮盖住

- **① 尺寸定死**：状态栏按最长状态文字预留、工具行与绘制面板叠放取高者、绘制面板压扁。
  实测 1000×1040 窗口下 11 次连续操作，数字键「5」下沿**全部是 886**
  （改之前进绘制模式会从 896 跳到 830）；窄窗口 460×900 下 XY 按钮 top 恒为 685。
- **② XY 按钮**：青绿描边圈出恰好两个候选数的格子。实测点 XY → 状态栏「已高亮 12 个只剩两个候选数的格子」，
  盘面统计到 2540 px 青绿描边；随后按数字键 1，青绿像素归零。
- **③ 优先级**：绘制模式不显示选中格；涂色优先级最高。实测浅色主题下涂 `#C8E6C9` 的格子
  72×72 像素全为涂色，退出绘制后再点它，「选中色」与「行列高亮」像素均为 0，涂色 4900 px 完整保留。

## 阶段六 · 提示高亮分色与分步渐显

- 新增标记模型（`Sudoku.Core/HintMarks.cs`）：角色（结构 / 候选数 ABC / 鱼鳍 / 结论）+ 出现的推导进度；
  棋盘跟着「下一步」逐段亮起。色表见 [`docs/DESIGN.md`](docs/DESIGN.md#三提示高亮分色与分步渐显)。
- 实测：导入一盘同时存在 XY 翼与带鳍 X 翼的中间盘面，选「XY 翼」时第 1 步只有绿 118 px + 红 105 px（枢轴），
  第 2 步紫 301 px（两翼加入），第 6 步才出现品红 487 px（删除圈叉）；换「带鳍 X 翼」时第 3 步出现橙色鳍格 540 px；
  点「应用这一步」后六种颜色全部归零。
- 顺手修掉：提示面板高度不再随推导文字变高（原来枢轴高亮块横坐标会从 161 漂到 224，修后稳定在 237~250）。

## 阶段五 · 大师档与底部提示面板

- **「大师」档**：解题路径上必须用到阶段四高阶技巧；简单~专家四档在出题校准时主动回填线索，
  保证不会需要这些技巧。生成时改用非对称挖洞 + 4 次重挖取最深（`maxAttempts: 200`）。
- **提示面板搬到底部**：手机上原来浮在九宫格上的提示框太占地方，改成贴底面板，打开时数字键盘/工具行整块让位。
- **修掉的 bug**：`Undo()` / `Redo()` 只发棋盘重绘、没发 `CanUndo/CanRedo` 属性通知，
  所以刚点完「撤销」时「重做」还是灰的，看着像没功能；改走 `NotifyStateChanged()` 后一并修好。
- 实测：点「提示」后数字键与底部工具按钮在 UIA 里全部查不到（`5` / `绘制` missing），点「关闭」立刻恢复；
  面板高度占窗口比：984×1032 → 249 px（24%），414×822 → 188 px（23%），「收起」后 94 px（9~11%）；
  点「大师」1.1 秒出题，标题「大师 · 5.7」，存档 `LevelValue=4 / TechniqueLevel=7`。

## 阶段四 · 高阶技巧

- 新增 5 种技巧（带鳍 X 翼、带鳍剑鱼、唯一矩形、BUG+1、远程数对），等级 7~10 接入求解器与提示；
  提示时在盘面画出箭头、强弱链、涉及格与删除标记。
- 新增设置「高阶提示」（默认关）与「应用后清除提示绘制」（默认开）。
- 实测：关掉「高阶提示」时点提示直接给「提示：R1C3 应填 5」；打开后技巧列表里能看到 X 翼 / XY 翼 /
  剑鱼 / 远程数对 / AIC 等，选中路径后棋盘出现描边、强弱链与删除标记（同一提示框位置下棋盘区差约 23 万像素、
  强链蓝像素 0 → 3770）；「应用后清除」开启时盘面图示消失（提示底色像素 6200 → 1480），关掉则保留。

## 阶段三 · 棋盘交互改造

去橡皮 / 随屏幕自适应的提示面板 / 数字锁定模式与候选数高亮 / 双击快速填入（Windows）/
自动标记改开关 / 上方对局管理按钮 / **绘制模式**（涂色 + 带箭头强弱链、6 色、线条改细）。

## 阶段二 · 技法实验室

| 子项 | 内容 |
| --- | --- |
| 2.1 技法引擎扩展 | 扩展到链级：X 翼、剑鱼、XY 翼、X 链、XY 链、（含 AIC），等级 6~9 接入求解器；每步附中文推导与强弱链数据 |
| 2.2 分段式提示 | 列技巧 → 选路径 → 逐步推进展示推导与删除；可限制提示技法上限 |
| 2.3 强弱链画线 | 格内候选数自由画线，实线强链 / 虚线弱链，可撤销、随存档保存（阶段三并入「绘制」模式） |
| 2.4 难度评分 | SER 风格评分，新增「专家」档；标题栏显示「档位 · 评分」 |
| 2.5 盘面导入导出 | SDK 文本格式（兼容 Hodoku 谜题）导入导出，导入自动校验合法性 / 有解 / 唯一解并评分 |

## 阶段一 · 可玩版本

出题（唯一解 + 中心对称挖洞）、自动标记候选数、数字面板与笔记模式、撤销/重做、
高亮与冲突提示、计时与剩余格数、逐步自动存档与「继续上一局」、深浅双主题、错误次数上限、
卡片式结果浮层、Windows 物理键盘操作。
